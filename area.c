#include <stdio.h>

int main(void) {
    int length = 7;
    int breadth = 4;
    int area = length * breadth;  // A = l * b

    printf("Length = %d\n", length);
    printf("Breadth = %d\n", breadth);
    printf("Area = %d\n", area);

    return 0;
}

